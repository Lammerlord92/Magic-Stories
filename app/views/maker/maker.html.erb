<div>
  <style>
    #graph_edit{
      width: 100%;
      height: 400px;
      border: 1px solid lightgrey;
    }
  </style>
  <div id="graph_edit"></div>
  <script type="text/javascript" charset="utf-8">
    var container = $("#graph_edit");

    // TODO: acordarse de borrar estos datos de ejemplo
    // create an array with nodes
    var nodes = new vis.DataSet([
      {id: 1, label: 'Node 1'},
      {id: 2, label: 'Node 2'},
      {id: 3, label: 'Node 3'},
      {id: 4, label: 'Node 4'},
      {id: 5, label: 'Node 5'}
    ]);

    // create an array with edges
    var edges = new vis.DataSet([
      {from: 1, to: 3},
      {from: 1, to: 2},
      {from: 2, to: 4},
      {from: 2, to: 5}
    ]);

    //  vis.Network(container, data, options)
    //    container es un elemento html
    //    data es un objeto con las propiedades nodes y edges
    var grafo = new vis.Network(container, {edges: edges, nodes: nodes}, {});

  </script>
</div>