<div class="storylist">
  <h1>Story List</h1>
  <% @stories.each do |story| %>
      <a href="/stories/<%= story.id %>"><%= story.title %></a>
      <br>
  <% end %>
</div>

<form>
  <input name=”q” <%= @q %> >
  <button type=”submit”>Buscar</button>
</form>