<div class="storyCreatedlist">
  <h1>Story List</h1>
  <% @stories.each do |story| %>
      <a href="/stories/<%= story.id %>"><%= story.title %><%= story.release_date %></a>
      <br>
  <% end %>
</div>