<% @categories.each do |category|%>
  <p>
    <%= link_to category.name, category,title: category.description, rel: 'tooltip' %>
    <%= image_tag category.icon.url(:icon) %>
    -<%= link_to "Borrar",category, method: :delete %>
  </p>
<% end %>

<%= link_to '<button type="button">Nueva categoría</button>'.html_safe, new_category_path %>


